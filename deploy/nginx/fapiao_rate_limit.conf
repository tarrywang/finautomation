# /etc/nginx/conf.d/fapiao_rate_limit.conf
# Must live in http {} context (not server {}).
# 10MB shared cache + 10 r/s, allow burst=5 in nodelay.

limit_req_zone $binary_remote_addr zone=fapiao_login:10m rate=10r/m;
